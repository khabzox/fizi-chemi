# Use the npm registry
registry=https://registry.npmjs.org/

# Enable workspaces
workspaces=true

# Ensure exact versioning for installed packages
save-exact=true

# Enable Turborepo caching to optimize build processes
turbo:loglevel=silent

# Use a cache directory to speed up installations
cache=~/.npm/cache

# Strict SSL for security
strict-ssl=true

# Optional: If using a private registry or scopes
# @your-scope:registry=https://your-private-registry-url/

# Optional: Set a max number of parallel jobs (default is 4)
# max-workers=4

# Optional: Silence unnecessary npm warnings
# loglevel=error
